{
  "name": "sudoku-under-the-stars",
  "version": "1.0.0",
  "private": true,
  "description": "Sudoku Under the Stars — 88 constellations, one puzzle each.",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "apk:debug": "cd android && ./gradlew assembleDebug",
    "apk:release": "cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/ios": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
